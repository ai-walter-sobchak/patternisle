/**
 * Arena mode flags. Arena v1 uses timed match only; shard/objective events
 * award points but do not end the round — only the match timer does.
 */
export const ARENA_V1_TIMED_MATCH_ONLY = true;
